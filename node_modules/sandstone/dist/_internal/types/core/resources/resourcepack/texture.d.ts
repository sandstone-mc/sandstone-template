import { type MCDocToJSON, type SymbolResource, type TEXTURE_TYPES } from '../../../arguments/index.js';
import type { LiteralUnion } from '../../../utils.js';
import { ContainerNode } from '../../nodes.js';
import type { SandstoneCore } from '../../sandstoneCore.js';
import type { ResourceClassArguments, ResourceNode } from '../resource.js';
import { ResourceClass } from '../resource.js';
type TextureType = LiteralUnion<TEXTURE_TYPES>;
type TextureMetaAll = MCDocToJSON<SymbolResource['texture_meta']>;
type TextureMeta<Type extends TextureType> = (Type extends 'entity/villager' ? Omit<Omit<TextureMetaAll, 'texture'>, 'gui'> : Type extends 'colormap' ? Omit<Omit<TextureMetaAll, 'villager'>, 'gui'> : Type extends 'gui' ? Omit<Omit<TextureMetaAll, 'villager'>, 'texture'> : Omit<Omit<Omit<TextureMetaAll, 'villager'>, 'texture'>, 'gui'>);
/**
 * A node representing a Minecraft texture.
 */
export declare class TextureNode<Type extends TextureType> extends ContainerNode implements ResourceNode<TextureClass<Type>> {
    resource: TextureClass<Type>;
    constructor(sandstoneCore: SandstoneCore, resource: TextureClass<Type>);
    getValue: () => ArrayBuffer | Buffer<ArrayBufferLike> | Promise<ArrayBuffer | Buffer<ArrayBufferLike>> | undefined;
}
export type TextureArguments<Type extends TextureType> = {
    /**
     * The texture's buffer.
     */
    texture?: Promise<ArrayBuffer | Buffer> | ArrayBuffer | Buffer;
    sprite?: boolean | string;
    meta?: TextureMeta<Type>;
} & ResourceClassArguments<'default'>;
export declare class TextureClass<Type extends TextureType> extends ResourceClass<TextureNode<Type>> {
    static readonly resourceType = "texture";
    type: Type;
    isSprite: boolean;
    spriteTarget?: string;
    meta?: TextureMeta<Type>;
    buffer: TextureArguments<Type>['texture'];
    constructor(core: SandstoneCore, type: Type, name: string, args: TextureArguments<Type>);
    toString(): string;
    videoToAnimation(_path: string): void;
}
export {};
//# sourceMappingURL=texture.d.ts.map