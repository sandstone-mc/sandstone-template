import type { VectorClass } from "../../../variables/index.js";
import type { Coordinates, ContainerSlotSelector, EntitySlotSelector, MultipleEntitiesArgument, Registry } from '../../../arguments/index.js';
import type { SandstoneCore } from '../../../core/sandstoneCore.js';
import type { ItemPredicateClass } from '../../../variables/ItemPredicate.js';
import { SingleConditionNode } from '../condition.js';
/** Item predicate for matching items in inventory slots. */
export type ItemPredicate = Registry['minecraft:item'] | ItemPredicateClass;
/**
 * Condition node for testing items in a block entity's inventory slots.
 *
 * @example
 * ```ts
 * // Check if chest has any diamonds
 * _.if(_.items.block(abs(0, 64, 0), 'container.*', 'minecraft:diamond'), () => { ... })
 * ```
 */
export declare class ItemsBlockConditionNode extends SingleConditionNode {
    private sourcePos;
    private slots;
    private itemPredicate;
    constructor(sandstoneCore: SandstoneCore, sourcePos: Coordinates, slots: ContainerSlotSelector, itemPredicate: ItemPredicate);
    getCondition(): (string | /*                                  */VectorClass<[string, string, string]>)[];
}
/**
 * Condition node for testing items in an entity's inventory slots.
 *
 * @example
 * ```ts
 * // Check if player has any diamonds
 * _.if(_.items.entity('@p', 'inventory.*', 'minecraft:diamond'), () => { ... })
 * ```
 */
export declare class ItemsEntityConditionNode extends SingleConditionNode {
    private source;
    private slots;
    private itemPredicate;
    constructor(sandstoneCore: SandstoneCore, source: MultipleEntitiesArgument, slots: EntitySlotSelector, itemPredicate: ItemPredicate);
    getCondition(): string[];
}
//# sourceMappingURL=items.d.ts.map