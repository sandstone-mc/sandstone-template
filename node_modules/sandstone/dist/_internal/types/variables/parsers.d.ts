import type { Coordinates, Range, Rotation, STRUCTURE_MIRROR, STRUCTURE_ROTATION } from '../arguments/index.js';
import type { SandstoneCore } from '../core/sandstoneCore.js';
import { type MacroArgument } from '../core/Macro.js';
import { VectorClass } from './Coordinates.js';
export declare function coordinatesParser<T>(coordinates: T): T extends Coordinates<boolean> ? VectorClass<[string, string, string]> : T;
export declare function rotationParser<T>(rotation: T): T extends Rotation<boolean> ? VectorClass<[string, string]> : T;
export declare const sanitizeValue: (core: SandstoneCore, value: MacroArgument | number | string | null | undefined) => string;
export declare const rangeParser: (core: SandstoneCore, range: Range<boolean> | MacroArgument) => string;
export type StructureRotation = STRUCTURE_ROTATION | number | `${90 | 180 | 270}` | `-${90 | 180 | 270}`;
export declare const structureRotationParser: (rotation?: StructureRotation | MacroArgument) => MacroArgument | STRUCTURE_ROTATION;
export type StructureMirror = STRUCTURE_MIRROR | '^x' | '^z' | 'x' | 'z' | boolean;
export declare const structureMirrorParser: (mirror?: StructureMirror | MacroArgument) => STRUCTURE_MIRROR;
/**
 * Parses a target into a string representation.
 * If the target is a string, it is returned as is.
 * If the target has a `_toSelector` method, it is called to get the string representation.
 * If the target has a `toMacro` method, it is called to get the string representation.
 * Otherwise, the `toString` method is called to get the string representation.
 *
 * If the target is undefined, an error is thrown.
 * @param target The target to parse.
 * @returns The string representation of the target.
 */
export declare function targetParser(target: any): string;
//# sourceMappingURL=parsers.d.ts.map