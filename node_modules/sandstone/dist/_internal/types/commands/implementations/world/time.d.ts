import type { FinalCommandOutput } from "../../index.js";
import type { Registry, TimeArgument } from '../../../arguments/index.js';
import type { Macroable, WorldClockClass } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
import type { NamespacedLiteralUnion } from '../../../../index.js';
type WORLD_CLOCKS = ('overworld' | 'the_end' | 'minecraft:overworld' | 'minecraft:the_end');
export declare class TimeCommandNode extends CommandNode {
    command: "time";
}
export declare class TimeArguments<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof TimeCommandNode;
    /**
     * Adds `time` to the in-game daytime.
     *
     * @param time the time to add. It can be suffixed with a unit. Units include:
     * - `d`: an in-game day, 24000 gameticks;
     * - `s`: a second, 20 gameticks;
     * - `t` (default and omitable): a single gametick; the default unit.
     */
    add: (time: Macroable<TimeArgument, MACRO>) => /*                     */FinalCommandOutput;
    /**
     * Queries current time.
     *
     * @param type Specifies the time to query. Can be one of the following:
     * - `gametime`: the age of the world in game ticks. (the game time modulo 2147483647)
     * - `time`: an alias for the `minecraft:overworld` timeline
     * - Any data-driven timeline (eg. `minecraft:villager_schedule`)
     */
    query: (type: Macroable<"gametime" | "time" | Registry["minecraft:timeline"], MACRO>) => /*                     */FinalCommandOutput;
    /**
     * Sets the in-game daytime.
     *
     * @param time the new daytime. It can be suffixed with a unit. Units include:
     * - `d`: an in-game day, 24000 gameticks;
     * - `s`: a second, 20 gameticks;
     * - `t` (default and omitable): a single gametick; the default unit.
     */
    set: (time: Macroable<TimeArgument | NamespacedLiteralUnion<"day" | "night" | "noon" | "midnight">, MACRO>) => /*                     */FinalCommandOutput;
}
export declare class TimeCommand<MACRO extends boolean> extends TimeArguments<MACRO> {
    protected NodeType: typeof TimeCommandNode;
    /**
     * Operates with the time of a specific world clock
     */
    of: (worldClock: Macroable<NamespacedLiteralUnion<WORLD_CLOCKS> | WorldClockClass, MACRO>) => TimeArguments<boolean>;
}
export {};
//# sourceMappingURL=time.d.ts.map