import type { FinalCommandOutput } from "../../index.js";
import type { Coordinates, Rotation } from '../../../arguments/index.js';
import type { Macroable } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class SetWorldSpawnCommandNode extends CommandNode {
    command: "setworldspawn";
}
export declare class SetWorldSpawnCommand<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof SetWorldSpawnCommandNode;
    /**
     * Set world spawn point.
     *
     * @param pos Optional coordinates for world spawn.
     *           Defaults to current command execution position.
     *           Examples: abs(0, 70, 0), abs(100, 64, 200), rel(0, 5, 0)
     *
     * @param angle Optional yaw angle for spawn direction.
     *             Defaults to current executor facing direction.
     *             Examples: [90, 0], [180, -10]
     *
     * @example
     * ```ts
     * setworldspawn()                      // Set spawn at current location
     * setworldspawn(abs(0, 70, 0))           // Set spawn at coordinates
     * setworldspawn(abs(100, 64, 200), [90, 0])  // With specific direction
     * ```
     */
    setworldspawn: (pos?: Macroable<Coordinates<MACRO>, MACRO>, angle?: Macroable<Rotation<MACRO>, MACRO>) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=setworldspawn.d.ts.map