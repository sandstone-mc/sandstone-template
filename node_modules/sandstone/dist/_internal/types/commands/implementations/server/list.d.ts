import type { FinalCommandOutput } from "../../index.js";
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class ListCommandNode extends CommandNode {
    command: "list";
}
export declare class ListCommand extends CommandArguments {
    protected NodeType: typeof ListCommandNode;
    /**
     * Display list of online players.
     *
     * @param uuids Optional flag to show player UUIDs alongside names.
     *             Defaults to false (names only).
     *
     * @example
     * ```ts
     * list()          // Show player names
     * list(true)      // Show names and UUIDs
     * ```
     */
    list: (uuids?: boolean) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=list.d.ts.map