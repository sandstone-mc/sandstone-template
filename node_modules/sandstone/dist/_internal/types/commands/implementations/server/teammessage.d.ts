import type { FinalCommandOutput } from "../../index.js";
import type { MessageOrSelector } from '../../../arguments/index.js';
import { CommandNode } from '../../../core/nodes.js';
import type { AtLeastOne } from '../../../utils.js';
import { CommandArguments } from '../../helpers.js';
export declare class TeamMessageCommandNode extends CommandNode {
    command: "tm";
}
export declare class TeamMessageCommand extends CommandArguments {
    protected NodeType: typeof TeamMessageCommandNode;
    /**
     * Send message to your team.
     *
     * @param messages Message content to send to team members.
     *                Multiple arguments are joined with spaces.
     *                Can include entity selectors.
     *
     * @example
     * ```ts
     * teammessage('Enemy spotted at base!')         // Team chat message
     * teammessage('Found', '@e[type=zombie]', 'zombies')  // Message with selectors
     * teammessage('Need backup at coordinates 100 64 200')
     * ```
     */
    teammessage: (...messages: AtLeastOne<MessageOrSelector>) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=teammessage.d.ts.map