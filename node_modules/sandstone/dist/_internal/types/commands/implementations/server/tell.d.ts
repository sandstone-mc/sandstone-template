import type { FinalCommandOutput } from "../../index.js";
import type { MessageOrSelector, MultiplePlayersArgumentOf } from '../../../arguments/index.js';
import { CommandNode } from '../../../core/nodes.js';
import type { AtLeastOne } from '../../../utils.js';
import { CommandArguments } from '../../helpers.js';
export declare class TellCommandNode extends CommandNode<[MultiplePlayersArgumentOf<false, string>, AtLeastOne<MessageOrSelector>]> {
    command: "w";
}
export declare class TellCommand<_MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof TellCommandNode;
    /**
     * Send private message to players.
     *
     * @param targets Player selector to send message to.
     *               Examples: '@p', '@a', 'PlayerName'
     *
     * @param messages Message content to send. Multiple arguments joined with spaces.
     *                Can include entity selectors that get resolved to names.
     *
     * @example
     * ```ts
     * tell('@p', 'Hello there!')                    // Private message
     * tell('@a', 'Welcome', '@p', 'to the server')  // Message with selectors
     * tell('PlayerName', 'You have', '5', 'items')  // Multiple message parts
     * ```
     */
    tell: <T extends string>(targets: MultiplePlayersArgumentOf<false, T>, ...messages: AtLeastOne<MessageOrSelector>) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=tell.d.ts.map