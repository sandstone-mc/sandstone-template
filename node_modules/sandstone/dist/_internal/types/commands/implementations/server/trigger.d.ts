import type { FinalCommandOutput } from "../../index.js";
import type { ObjectiveArgument } from '../../../arguments/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class TriggerCommandNode extends CommandNode {
    command: "trigger";
}
export declare class TriggerArgumentsCommand extends CommandArguments {
    /**
     * Adds `value` to the current value of `objective`.
     *
     * @param value the value to add.
     */
    add: (value: number) => /*                     */FinalCommandOutput;
    /**
     * Sets the value of `objective` to `value`.
     *
     * @param value the new value.
     */
    set: (value: number) => /*                     */FinalCommandOutput;
}
export declare class TriggerCommand extends CommandArguments {
    protected NodeType: typeof TriggerCommandNode;
    /**
     * Modify trigger scoreboard objective.
     *
     * @param objective Trigger-type objective to modify.
     *                 Must be enabled for the player executing the command.
     *                 Examples: 'my_trigger', triggerObjective
     *
     * @example
     * ```ts
     * trigger('my_trigger')              // Add 1 to trigger
     * trigger('settings').add(5)         // Add 5 to settings trigger
     * trigger('menu').set(3)             // Set menu trigger to 3
     * trigger(myTriggerObj).add(10)      // Use Sandstone objective
     * ```
     */
    trigger: (objective: ObjectiveArgument) => TriggerArgumentsCommand;
}
//# sourceMappingURL=trigger.d.ts.map