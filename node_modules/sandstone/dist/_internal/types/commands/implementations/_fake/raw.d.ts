import type { FinalCommandOutput } from "../../index.js";
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class RawCommandNode extends CommandNode {
    command: "";
}
export declare class RawCommand extends CommandArguments<typeof RawCommandNode> {
    protected NodeType: typeof RawCommandNode;
    /**
     * Create arbitrary command with raw arguments.
     *
     * @param args Command arguments to join with spaces.
     *            Use for commands not supported by Sandstone or custom syntax.
     *
     * @example
     * ```ts
     * raw('custommod:special', 'arg1', 'arg2')     // custommod:special arg1 arg2
     * raw('execute', 'run', 'say', 'Hello')        // execute run say Hello
     * raw('debug', 'start')                        // debug start
     * ```
     */
    raw: (...args: unknown[]) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=raw.d.ts.map