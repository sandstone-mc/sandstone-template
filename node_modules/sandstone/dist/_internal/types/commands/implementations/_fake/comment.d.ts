import type { FinalCommandOutput } from "../../index.js";
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class CommentCommandNode extends CommandNode<[unknown[]]> {
    command: "#";
    getValue(): string;
}
export declare class CommentCommand extends CommandArguments<typeof CommentCommandNode> {
    protected NodeType: typeof CommentCommandNode;
    /**
     * Add comments to generated functions.
     *
     * @param comments Text to include as comments in the function.
     *                Multiple arguments are joined with spaces.
     *                Supports multi-line comments.
     *
     * @example
     * ```ts
     * comment('This is a helpful comment')       // # This is a helpful comment
     * comment('Setup phase', 'for minigame')     // # Setup phase for minigame
     * comment('Multi\nline\ncomment')            // # Multi
     *                                            // # line
     *                                            // # comment
     * ```
     */
    comment: (...comments: unknown[]) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=comment.d.ts.map