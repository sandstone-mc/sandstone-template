import type { FinalCommandOutput } from "../../index.js";
import type { SingleEntityArgumentOf, SinglePlayerArgumentOf } from '../../../arguments/index.js';
import type { Macroable } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class SpectateCommandNode extends CommandNode {
    command: "spectate";
}
export declare class SpectateCommand<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof SpectateCommandNode;
    /**
     * Make a spectator player follow an entity.
     *
     * @param target Entity to spectate.
     *              Examples: '@p', '@e[type=zombie, limit=1]', 'PlayerName'
     *
     * @param player Optional spectator player. Defaults to command executor.
     *              Examples: '@p', 'SpectatorName'
     *
     * @example
     * ```ts
     * spectate('@e[type=zombie, limit=1]')        // You spectate nearest zombie
     * spectate('@p', 'SpectatorPlayer')          // SpectatorPlayer spectates nearest player
     * spectate('@e[type=villager, limit=1]', '@p') // Nearest player spectates villager
     * ```
     */
    spectate: <T extends string, P extends string>(target: Macroable<SingleEntityArgumentOf<MACRO, T>, MACRO>, player?: Macroable<SinglePlayerArgumentOf<MACRO, P>, MACRO>) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=spectate.d.ts.map