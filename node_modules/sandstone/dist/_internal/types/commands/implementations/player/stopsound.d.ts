import type { FinalCommandOutput } from "../../index.js";
import type { MultiplePlayersArgumentOf, Registry, SOUND_SOURCES } from '../../../arguments/index.js';
import type { Macroable } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class StopSoundCommandNode extends CommandNode {
    command: "stopsound";
}
export declare class StopSoundCommand<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof StopSoundCommandNode;
    /**
     * Stop playing sounds for players.
     *
     * @param targets Player selector to stop sounds for.
     *               Examples: '@p', '@a', 'PlayerName'
     *
     * @param source Optional sound category to stop. Use '*' for all categories.
     *              Examples: 'master', 'music', 'ambient', 'block', '*'
     *
     * @param sound Optional specific sound to stop.
     *             Examples: 'minecraft:entity.pig.ambient', 'minecraft:block.note_block.harp'
     *
     * @example
     * ```ts
     * stopsound('@a')                                    // Stop all sounds for all players
     * stopsound('@p', 'music')                          // Stop music for nearest player
     * stopsound('@a', '*', 'minecraft:entity.pig.ambient') // Stop pig sounds for all
     * ```
     */
    stopsound: <T extends string>(targets: Macroable<MultiplePlayersArgumentOf<MACRO, T>, MACRO>, source?: Macroable<SOUND_SOURCES | "*", MACRO>, sound?: Macroable<Registry["minecraft:sound_event"], MACRO>) => /*                     */FinalCommandOutput;
}
//# sourceMappingURL=stopsound.d.ts.map