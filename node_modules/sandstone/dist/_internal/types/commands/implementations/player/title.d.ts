import type { FinalCommandOutput } from "../../index.js";
import type { JSONTextComponent, MultiplePlayersArgumentOf, TimeArgument } from '../../../arguments/index.js';
import type { Macroable } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class TitleCommandNode extends CommandNode {
    command: "title";
}
export declare class TitleArgumentsCommand<MACRO extends boolean> extends CommandArguments {
    clear: () => /*                     */FinalCommandOutput;
    reset: () => /*                     */FinalCommandOutput;
    title: (title: Macroable<JSONTextComponent, MACRO>) => /*                     */FinalCommandOutput;
    subtitle: (subtitle: Macroable<JSONTextComponent, MACRO>) => /*                     */FinalCommandOutput;
    actionbar: (actionbarText: Macroable<JSONTextComponent, MACRO>) => /*                     */FinalCommandOutput;
    times: (fadeIn: Macroable<TimeArgument, MACRO>, stay: Macroable<TimeArgument, MACRO>, fadeOut: Macroable<TimeArgument, MACRO>) => /*                     */FinalCommandOutput;
}
export declare class TitleCommand<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof TitleCommandNode;
    /**
     * Display title text on player screens.
     *
     * @param targets Player selector to show title to.
     *               Examples: '@p', '@a', 'PlayerName', '@a[team=red]'
     *
     * @example
     * ```ts
     * title('@a').title('Hello World')                    // Main title
     * title('@p').subtitle('Welcome back!')               // Subtitle
     * title('@a').actionbar('Health: 20/20')             // Action bar text
     * title('@p').times(10, 70, 20)                      // Fade in/stay/fade out ticks
     * title('@a').clear()                                 // Clear current title
     * ```
     */
    title: <T extends string>(targets: Macroable<MultiplePlayersArgumentOf<MACRO, T>, MACRO>) => TitleArgumentsCommand<MACRO>;
}
//# sourceMappingURL=title.d.ts.map