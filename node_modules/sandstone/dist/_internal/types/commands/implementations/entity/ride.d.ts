import type { FinalCommandOutput } from "../../index.js";
import type { SingleEntityArgumentOf } from '../../../arguments/index.js';
import type { Macroable } from '../../../core/index.js';
import { CommandNode } from '../../../core/nodes.js';
import { CommandArguments } from '../../helpers.js';
export declare class RideCommandNode extends CommandNode {
    command: "ride";
}
export declare class RideArgumentsCommand<MACRO extends boolean> extends CommandArguments {
    /**
     * Adds the target as a passenger of the mount.
     *
     * @param target Specifies the mount.
     */
    mount: <T extends string>(target: Macroable<SingleEntityArgumentOf<MACRO, T>, MACRO>) => /*                     */FinalCommandOutput;
    /**
     * Dismounts the target if it is mounted.
     */
    dismount: () => /*                     */FinalCommandOutput;
}
export declare class RideCommand<MACRO extends boolean> extends CommandArguments {
    protected NodeType: typeof RideCommandNode;
    /**
     * Control entity mounting and dismounting.
     *
     * @param target Entity to control mounting behavior for.
     *              Must be a single entity selector.
     *              Examples: '@p', '@e[type=horse, limit=1]', 'PlayerName'
     *
     * @example
     * ```ts
     * // Mount player on horse
     * ride('@p').mount('@e[type=horse, limit=1]')
     *
     * // Dismount player
     * ride('@p').dismount()
     *
     * // Mount specific entities
     * ride('@e[type=pig, limit=1]').mount('@e[type=chicken, limit=1]')
     * ride('PlayerName').mount('@e[type=boat, limit=1]')
     * ```
     */
    ride: <T extends string>(target: Macroable<SingleEntityArgumentOf<MACRO, T>, MACRO>) => RideArgumentsCommand<boolean>;
}
//# sourceMappingURL=ride.d.ts.map